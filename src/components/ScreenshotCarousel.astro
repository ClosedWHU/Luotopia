---
const slides = [
  { label: "课程表" },
  { label: "校园服务" },
  { label: "AI 助手" },
  { label: "天气" },
];
---

<div class="relative overflow-hidden rounded-[var(--md-sys-shape-corner-medium)] bg-[var(--md-sys-color-surface-container-low)] dark:bg-[var(--md-sys-color-surface-container-low-dark)]">
  <div id="carousel-track" class="flex transition-transform duration-500 ease-out">
    {slides.map((slide, i) => (
      <div class="min-w-full flex items-center justify-center aspect-[9/16] max-h-[500px] bg-[var(--md-sys-color-surface-container)] dark:bg-[var(--md-sys-color-surface-container-dark)]">
        <div class="text-center p-8">
          <div class="w-16 h-16 mx-auto mb-4 rounded-[var(--md-sys-shape-corner-large)] bg-[var(--md-sys-color-primary-container)] flex items-center justify-center">
            <svg class="w-8 h-8 text-[var(--md-sys-color-primary)]" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="3" y="3" width="18" height="18" rx="3"/><path d="M3 9h18"/><path d="M9 3v18"/></svg>
          </div>
          <p class="text-[var(--md-sys-typescale-title-medium-size)] font-[var(--md-sys-typescale-title-medium-weight)] text-[var(--md-sys-color-on-surface)]">{slide.label}</p>
          <p class="text-[var(--md-sys-typescale-body-small-size)] text-[var(--md-sys-color-on-surface-variant)] mt-1">截图待添加</p>
        </div>
      </div>
    ))}
  </div>

  <button id="carousel-prev" class="absolute left-2 top-1/2 -translate-y-1/2 p-2 rounded-full bg-[var(--md-sys-color-surface-container-high)] dark:bg-[var(--md-sys-color-surface-container-high-dark)] text-[var(--md-sys-color-on-surface-variant)] hover:text-[var(--md-sys-color-primary)] transition-colors cursor-pointer shadow-[var(--md-sys-elevation-level2)]" aria-label="上一张">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
  </button>
  <button id="carousel-next" class="absolute right-2 top-1/2 -translate-y-1/2 p-2 rounded-full bg-[var(--md-sys-color-surface-container-high)] dark:bg-[var(--md-sys-color-surface-container-high-dark)] text-[var(--md-sys-color-on-surface-variant)] hover:text-[var(--md-sys-color-primary)] transition-colors cursor-pointer shadow-[var(--md-sys-elevation-level2)]" aria-label="下一张">
    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg>
  </button>

  <div class="absolute bottom-3 left-1/2 -translate-x-1/2 flex gap-1.5">
    {slides.map((_, i) => (
      <button class="carousel-dot w-2 h-2 rounded-full bg-[var(--md-sys-color-outline)] transition-all cursor-pointer data-[active=true]:w-4 data-[active=true]:bg-[var(--md-sys-color-primary)]" data-index={i} aria-label={`第 ${i + 1} 张截图`}></button>
    ))}
  </div>
</div>

<script>
  (function () {
    const track = document.getElementById("carousel-track");
    const prev = document.getElementById("carousel-prev");
    const next = document.getElementById("carousel-next");
    const dots = document.querySelectorAll(".carousel-dot");
    if (!track || !prev || !next) return;

    let current = 0;
    const total = dots.length;

    function update() {
      track.style.transform = `translateX(-${current * 100}%)`;
      dots.forEach((dot, i) => dot.dataset.active = i === current);
    }

    prev.addEventListener("click", () => { current = (current - 1 + total) % total; update(); });
    next.addEventListener("click", () => { current = (current + 1) % total; update(); });
    dots.forEach((dot) => {
      dot.addEventListener("click", () => { current = parseInt(dot.dataset.index ?? "0"); update(); });
    });

    let autoplay = setInterval(() => { current = (current + 1) % total; update(); }, 5000);
    const container = track.parentElement;
    container?.addEventListener("mouseenter", () => clearInterval(autoplay));
    container?.addEventListener("mouseleave", () => { autoplay = setInterval(() => { current = (current + 1) % total; update(); }, 5000); });
  })();
</script>
